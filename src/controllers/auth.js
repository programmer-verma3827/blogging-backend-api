const Base = require("./base");

class Auth extends Base {
          constructor(req, res, next) {
                    super(req, res, next);
          }

          login() {

          }

          register() {
                    
          }
}

module.exports = Auth;